/target
/dist
# Plugin examples and the Rust SDK are built on their own (plugins/*/build.sh)
/plugins/*/target
/sdk/rust/target
# Built modules: the source is what is committed
*.wasm
.DS_Store
*.profraw
# Left behind by the Python scanners and hooks
__pycache__/
.env.*
!.env.example
# Credentials — never commit
.env
*.pem
*.p12
*.key
*.cer
*.mobileprovision
