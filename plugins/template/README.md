# {{name}}

An [Agentty](https://www.agentty.run) plugin.

- `agentty-plugin.json` — manifest (id, commands, panel, permissions)
- `main.mjs` — the plugin process
- `agentty-plugin.mjs` / `agentty-plugin.d.ts` — the SDK and its types
- `PLUGIN_GUIDE.md` — the full developer guide

After editing, press **Restart** on the plugin in Agentty's Plugins page.
