name: CI

# Pull requests only: pushes to main (releases included) are checked locally by the pre-push hook
# and the release skill's checks.
on:
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  # These jobs run on the self-hosted Apple silicon Mac, check-windows on a self-hosted Windows PC
  # (the repository is private, so only collaborators' pull requests reach them).
  secrets:
    runs-on: [self-hosted, macOS, ARM64]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - name: Scan tracked files for credentials
        run: python3 scripts/check-secrets.py --all
      - name: Security audit (forbidden files, personal data, weakened guarantees, guard wiring)
        run: python3 scripts/security-audit.py --all
      - name: Scan commits in this pull request
        run: python3 scripts/check-secrets.py --range "${{ github.event.pull_request.base.sha }}..${{ github.sha }}"

  check:
    needs: secrets
    runs-on: [self-hosted, macOS, ARM64]
    env:
      # Kept next to the checkout, not in it: checkout cleans the work tree, and the runner's own
      # disk is the cache (Swatinem/rust-cache deleted rustup from the runner's ~/.cargo/bin).
      CARGO_TARGET_DIR: ${{ github.workspace }}/../agentty-target
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      # The toolchain (and its rustfmt / clippy) comes from rust-toolchain.toml, same as locally.
      - name: Install toolchain
        run: rustup toolchain install
      - name: Format
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Test
        run: cargo test --workspace
      - name: Build (release)
        run: cargo build --release -p agentty-app

  # Windows can't be built on the Mac (GPUI compiles its shaders with the Windows SDK's fxc.exe).
  # The runner needs rustup, git and Visual Studio Build Tools (C++ workload + Windows SDK). The zip
  # is uploaded as a workflow artifact: download it from the run's Summary page.
  check-windows:
    needs: secrets
    runs-on: [self-hosted, Windows, X64]
    env:
      CARGO_TARGET_DIR: ${{ github.workspace }}\..\agentty-target
    # Windows PowerShell refuses the runner's step scripts under the default execution policy
    # (Restricted); bypass it for these steps only instead of changing the PC's policy.
    defaults:
      run:
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Install toolchain
        run: rustup toolchain install
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Test
        run: cargo test --workspace
      # No install round trip here: the runner is a personal PC, and install.ps1 -Uninstall would
      # remove an Agentty the user installed on it.
      - name: Package
        run: |
          ./scripts/package-windows.ps1
          $info = (Get-Item "$env:CARGO_TARGET_DIR\release\agentty.exe").VersionInfo
          if ($info.ProductName -ne 'Agentty') { throw "version resource missing: '$($info.ProductName)'" }
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: agentty-windows-x86_64
          path: |
            dist/agentty-*-windows-*.zip
            dist/agentty-*-windows-*.zip.sha256
          if-no-files-found: error
          retention-days: 14

  # Linux build: planned, not supported yet. Uncomment this job to re-enable it (it passed on the
  # merge of PR #3).
  # check-linux:
  #   needs: secrets
  #   runs-on: ubuntu-24.04
  #   steps:
  #     - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
  #     - name: Install system libraries
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y --no-install-recommends pkg-config libxkbcommon-dev libxkbcommon-x11-dev \
  #           libwayland-dev libx11-xcb-dev libxcb1-dev libvulkan-dev libfontconfig-dev libfreetype-dev libzstd-dev zsh
  #     - name: Install toolchain
  #       run: rustup toolchain install
  #     - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
  #     - name: Clippy
  #       run: cargo clippy --workspace --all-targets -- -D warnings
  #     - name: Test
  #       run: cargo test --workspace
  #     - name: Build (release)
  #       run: cargo build --release -p agentty-app
  #     - name: Package and install round trip
  #       run: |
  #         scripts/package-linux.sh
  #         tar -xzf dist/agentty-*-linux-*.tar.gz -C target
  #         prefix="$RUNNER_TEMP/agentty-prefix"
  #         PREFIX="$prefix" target/agentty-*-linux-*/install.sh
  #         test -x "$prefix/bin/agentty"
  #         test -f "$prefix/share/applications/run.agentty.Agentty.desktop"
  #         PREFIX="$prefix" target/agentty-*-linux-*/install.sh --uninstall
  #         test ! -e "$prefix/bin/agentty"
