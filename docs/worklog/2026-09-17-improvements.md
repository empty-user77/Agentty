# 2026-09-17 개선 작업 목록

상태: ⬜ 대기 · 🔧 진행 중 · ✅ 완료 · ⚠️ 부분 완료/제약

| # | 분류 | 항목 | 상태 | 처리 내용 · 테스트 결과 |
|---|---|---|---|---|
| 1 | 사용량 | 차트 마우스 오버 툴팁 | ✅ | 차트 위 마우스 이동 시 가장 가까운 날짜의 비용/호출/입력/출력/캐시 툴팁 + 세로 가이드선. 합성 마우스 이동으로 표시 확인 |
| 2 | 사용량 | 모델 선택 드롭다운이 카드에 가려짐 | ✅ | 드롭다운을 deferred 레이어로 렌더링해 카드 위에 표시, 재클릭 시 닫힘 처리 |
| 3 | 세션 | 이어하기/세션 목록에 AI 공식 로고(전체 동일 적용) | ✅ | 공통 `brand` 모듈(Simple Icons CC0 로고: Claude/OpenAI(Codex)/Gemini/Copilot/Cursor/Qwen/Kimi/Cline/Grok/Ollama/OpenCode, 로고 없는 agy·Amp는 브랜드색 이니셜)로 이어하기 바, 세션 목록/뷰어, 탭, 분할창 헤더, AI 상태바, 런처, 시작 페이지, 알림, 미니모드, 세션연결 노드 전부 교체 |
| 4 | 세션 | 이어하기·최근 세션에 마지막 프롬프트/결과 요약 | ✅ | 세션 목록 정보에 마지막 프롬프트/마지막 답변 추가(파일 끝에서 읽고 mtime 캐시). 실제 로컬 세션으로 출력 확인 |
| 5 | UX | 새로고침 등 클릭 인터랙션 로딩 애니메이션 | ✅ | 공용 스피너/페이드인 추가: 세션 새로고침, 사용량 새로고침, AgentGit 새로고침·작업 중, MCP/스킬/브랜치/세션 기록 로딩, 업데이트 확인 중에 회전 아이콘. 런처·상태 메뉴·브랜치 메뉴·에이전트 패널·토스트는 120ms 페이드인 |
| 6 | 사이드바 | 워크스페이스/로컬세션 열고 닫기는 별도 아이콘으로 | ✅ | 활동바 아이콘은 패널을 열기만 하고(재클릭으로 닫히지 않음), 사이드바 헤더에 접기 아이콘(⌘B) 추가 |
| 7 | 세션연결 | 세션연결 화면에서 워크스페이스 사이드바 열림, 활성 표시는 세션연결만 | ✅ | 세션연결/AgentGit 페이지에서도 워크스페이스 사이드바 유지, 활동바 강조는 열린 페이지 하나만 |
| 8 | 워크스페이스 | 홈 디렉토리 `~` 표시를 전체 경로로 | ✅ | 홈 폴더는 `~` 대신 `/Users/ray` 전체 경로로 표시, 하위 폴더는 `~/Desktop` 유지 |
| 9 | 설정 | 예약어 설정에서 바꿀 값 입력 불가 | ✅ | 원인: 바꿀 값 입력칸(flex_1)의 텍스트 요소 폭이 0으로 계산돼 클릭이 입력칸에 닿지 않음. TextInput을 flex 확장 + 입력칸 전체 클릭 시 포커스로 수정. 합성 클릭/타이핑으로 두 칸 입력·추가 확인 |
| 10 | 설정 | 설정 변경 시 우측 상단 토스트 | ✅ | 설정 리비전 카운터로 설정 페이지에서 값이 바뀌면 우측 상단에 “설정이 업데이트되었습니다” 토스트(1.8초). 토글 클릭으로 확인 |
| 11 | 설정 | 시스템 언어 기본값, 미지원 시 English | ✅ | 언어에 “시스템 설정” 추가(기본값). macOS 언어가 ko/ja/zh면 해당 언어, 그 외 English. 단위 테스트 추가 |
| 12 | 단축키 | 워크스페이스(터미널) 화면에서 ⌘N → 새 탭 | ✅ | ⌘N 기본 동작 = 현재 탭 경로로 새 탭 (단축키 가이드 반영) |
| 13 | 탭 | 탭 바 빈 공간 우클릭 → + 메뉴 | ✅ | 탭 바 빈 공간 우클릭 시 + 메뉴와 같은 런처를 클릭 위치에 표시 |
| 14 | 단축키 | 워크스페이스 사이드바 포커스에서 ⌘N → 새 워크스페이스 | ✅ | 워크스페이스 목록에 포커스가 있을 때(목록 클릭 후) ⌘N = 새 워크스페이스 시작 페이지 (키 컨텍스트 `Sidebar`) |
| 15 | 드롭다운 | 아이콘 재클릭 시 드롭다운 닫힘(전체 공통) | ✅ | 원인: 바깥 클릭으로 닫힌 뒤 같은 클릭이 토글 버튼에 도달해 다시 열림. 공통 `note_dismissed/just_dismissed` 가드로 알림·런처·워크스페이스 메뉴·상태 메뉴·이어하기 메뉴·에이전트 패널·사용량 모델 메뉴에 적용 (브랜치 메뉴는 기존 가드) |
| 16 | 메뉴바 | 메뉴바 Agentty 메뉴에 AI별 사용량/초기화 시간 | ✅ | 메뉴바 메뉴에 “사용량 · 최근 7일” 섹션: Claude/Codex별 비용 + 주간(없으면 5시간) 사용률 + “초기화: N일 N시간 남음”. Claude 한도는 statusline 입력(rate_limits)을 저장, Codex는 rollout의 rate_limits 사용. 클릭 시 사용량 페이지. 실데이터 확인: Claude $289 / Codex 주간 89%·초기화 1일 22시간 |
| 17 | AgentGit | Git → AgentGit 이름 변경, 기록 diff가 사라지는 버그 | ✅ | 메뉴명 AgentGit(4개 언어). 원인: 3초 폴링이 작업트리 diff를 다시 불러오며 기록 탭의 커밋 diff를 지움 → 변경 탭일 때만 갱신. 6초 이상 폴링 후에도 diff 유지 확인 |
| 18 | 메트릭 | 오픈소스 방식 메트릭 수집 설계/도입 | ✅ | opencode/cmux/Zed/Warp/Ghostty/Homebrew 조사 후 결정: 옵트인(기본 꺼짐), DO_NOT_TRACK 존중, 허용 목록 이벤트만(경로·명령·프롬프트 수집 안 함), 로컬 JSONL 우선, 엔드포인트는 사용자가 설정할 때만 업로드(저장소에 키 없음). 설정 > 일반 > 개인정보, docs/metrics.md. 꺼짐 상태 무기록/켜짐 상태 기록 확인 |
| 19 | AgentGit | 진입 시 워크스페이스 사이드바, 워크스페이스 선택 → 저장소 연동(여러 개면 선택) | ✅ | AgentGit 진입 시 사이드바 유지. 워크스페이스 클릭 시 페이지 유지 + 해당 워크스페이스 저장소로 전환, 저장소가 여러 개면 “현재 저장소” 목록 자동 표시. 2개 저장소 워크스페이스로 확인(repos=2) |
| 20 | 미니모드 | 작업중 최상단, 최근 5개, 더보기 확장 | ✅ | 작업중 → 권한/답변 대기 → 최근 활동 순 정렬, 대기 외 최근 5개 + “더 보기(N)” 누르면 확장(창 높이 증가)/접기. 정렬 단위 테스트 |
| 21 | 미니모드 | 헤더에 Agentty 로고 | ✅ | 미니모드 헤더 원형 점 → Agentty 로고(작업 상태 점은 로고 모서리에 작게) |
| 22 | 탭 | 새 탭은 현재 탭 경로로 즉시(설정: 새 탭 열 때 항상 디렉토리 묻기) | ✅ | 새 탭(+, ⌘T, ⌘N, 더블클릭)은 현재 탭 경로로 즉시 생성. 설정 “새 탭 열 때 항상 디렉토리 위치 묻기”(기본 꺼짐), 워크스페이스용 폴더 묻기는 별도 설정 |
| 23 | 알림 | 권한 요청/답변 요청 감지 → 알림 + 카드 표시 | ✅ | 권한 요청(PermissionRequest 훅 + Claude/Codex 승인 화면 감지)·답변 요청(선택 질문 화면/elicitation 알림)을 별도 상태로 표시하고 알림/미니 말풍선/카드 배지 표시. idle 알림은 중복 알림에서 제외 |
| 24 | 세션 | 로컬 세션 클릭 → 기록 뷰어 + 새 워크스페이스/새 탭에서 이어하기 | ✅ | 세션 행 클릭 시 오른쪽에 대화 기록 뷰어(최근 200턴, 도구 호출은 칩), “새 워크스페이스에서 이어하기”/“현재 워크스페이스 새 탭에서 이어하기” 버튼 |
| 25 | 터미널 | 파일/이미지 드래그앤드롭 → 경로 입력 | ✅ | GPUI 기본 드롭 경로 대신 창의 AppKit 드래그 처리기를 교체해 모든 외부 드롭을 받고 포인터 아래 분할창에 경로 입력. Finder 파일(공백 경로 따옴표 처리), 스크린샷 썸네일 같은 파일 프라미스, 이미지 데이터(TIFF는 PNG로 변환) 모두 실제 드래그로 확인 |
| 26 | 상태 | Esc 취소 시 “작업 중” 유지 → “일시중단” | ✅ | Esc 입력·“Interrupted” 화면·트랜스크립트 중단 기록으로 “일시중단” 상태. 화면에서 작업 표시가 사라지면 작업 중 해제(시간 정지) |
| 27 | 상태바 | AI 상태 헤더 반응형(Usage/시간 숨김, 분할 아이콘 제거) | ✅ | 폭에 따라: Usage는 700px 미만 숨김, 경과시간 560px 미만 숨김, 경로는 820px 이상만 표시. 모델/컨텍스트/브랜치는 항상. 분할창 헤더의 분할 아이콘 제거(강조보기·닫기만) |
| 28 | 상태바 | exit/⌃D 후 헤더가 최근 세션 바로 전환되지 않음 | ✅ | 원인: Codex는 node로 실행돼 전경 프로세스 판별 실패, Claude는 종료 감지가 1초 폴링 의존. 프로세스 그룹 전체+실행 인자 검사로 판별하고 SessionEnd 훅으로 즉시 전환. 실제 Claude `/exit` 후 상태바 해제 확인 |
| 29 | 설정 | 최근 세션 바 / AI CLI 상태 바 ON/OFF | ✅ | 설정 > 일반에 “최근 세션바”, “AI CLI 상태바” ON/OFF(기본 ON, 설명 포함) |
| 30 | 상태바 | 브랜치 변경사항 `*`, 미푸시 표시 | ✅ | 3초마다 백그라운드 git status로 `main*`(변경 있음), `↑N`(미푸시 커밋) 표시. 테스트 저장소에서 `main *` 표시 확인 |
| 31 | 터미널 | 입력 시 글자가 겹쳐 보임 | ✅ | 원인 추정: 한글 IME 조합 중 커밋된 글자가 에코되기 전 조합 글자를 이전 커서 위치에 그려 겹침. 커밋 폭만큼 조합 위치를 밀어 그리도록 수정(동아시아 전각 폭 계산 테스트) |
| 32 | 세션연결 | 탭/터미널에서 연결 상태 확인·제어 | ✅ | 연결된 탭에 링크 아이콘(실시간이면 강조), 분할창 헤더/AI 상태바에 연결 칩 → 연결 패널: 보냄/받음 방향, 상대 탭 위치·상태, 공유 턴 수·대기 프롬프트, 실시간 연결 토글·다시 공유·이동·연결 해제 |
| 33 | 멀티에이전트 | 하네스/서브에이전트 시각화 | ✅ | SubagentStart/Stop 훅 + `<세션>/subagents/*.jsonl`·meta.json으로 서브에이전트 목록(종류·설명·모델·실행/완료·마지막 도구)과 선택한 에이전트 로그 표시. 실행 중이면 칩이 주황색 `1/3`. 이 세션의 실제 서브에이전트로 파싱 확인 |
| 34 | 분할 | 분할창 강조 보기(일시 확대) | ✅ | 분할창 헤더 “강조해서 보기” 아이콘/⇧⌘↩: 해당 창 78% 비율로 일시 확대, 다시 누르면 복원 |
| 35 | 터미널 | URL/파일 경로 클릭 → 브라우저/Finder, URL 파란색 | ✅ | 화면의 URL은 파란색, 마우스를 올린 링크/경로는 밑줄. 클릭(드래그 없이) 또는 ⌘클릭 시 URL은 브라우저(설정에 따라 인앱/외부), 파일 경로(`src/main.rs:12` 포함)는 Finder에서 표시. URL/경로 파싱 테스트 |
| 36 | UX | 아이콘 메뉴 툴팁(지연, 단축키 포함 짧은 설명) | ✅ | 아이콘 전용 버튼에 1.2초 지연 툴팁(이름 + 단축키): 탭 바 아이콘/+/알림, 사이드바 버튼, 분할창 강조·닫기, 상태바 스킬/에이전트/MCP, 찾기 바, AgentGit 아이콘, 연결 칩 (활동바 제외). 인앱 브라우저 툴바는 네이티브 웹뷰가 툴팁을 가려 제외 |
| 37 | 워크스페이스 | 드래그앤드롭 순서 변경, 최근 완료 워크스페이스 맨 위 | ✅ | 워크스페이스를 다른 카드 위로 드래그하면 그 위치(그룹 포함)로 이동. 에이전트 작업 완료 시 해당 워크스페이스를 맨 위로 |
| 38 | 워크스페이스 | 카드에 마지막 활동 시간 | ✅ | 카드 우측에 마지막 활동 시간(now/5m/2h), 호버 시 메뉴 버튼으로 전환 |
| 39 | 그룹 | 드래그앤드롭 순서 변경 | ✅ | 그룹 헤더를 다른 그룹 헤더로 드래그해 순서 변경 |
| 40 | 닫기 | 탭/분할창/워크스페이스 닫기 확인 + 다시 묻지 않기 + 설정, 빈 탭 예외 | ✅ | 입력/작업 이력이 있는 분할창·탭·워크스페이스만 확인창(다시 묻지 않기 체크), 설정 “탭/분할창/워크스페이스 닫을 때 묻기”. 아무것도 안 한 새 터미널/Claude/Codex는 바로 닫힘 |
| 41 | 에이전트 | Antigravity CLI(agy) 지원 | ⚠️ | `agy`를 주요 에이전트로 추가(런처·시작 페이지·감지·로고 이니셜·프로세스 감지). 이 Mac에는 agy가 설치돼 있지 않아 실행 확인은 못 함 — 설치(`~/.local/bin/agy`) 시 자동 표시 |
| 42 | 워크스페이스 | 새 워크스페이스 → 시작 페이지 + 이름 지정 | ✅ | 새 워크스페이스(+, ⌘N 목록, 런처)는 시작 페이지를 열고 선택적으로 이름 입력 후 원하는 AI/터미널로 시작, 취소 버튼 |
| 43 | 워크스페이스 | 카드에 사용 중 AI 로고 겹침 아바타(+N) | ✅ | 카드 앞에 사용 중인 도구 로고를 겹친 아바타로(최대 2개 + `+N`), AI 우선·터미널은 뒤, 제목 최소폭 유지 |
| 44 | 상태바/사이드바 | 터미널 아이콘 제거 → + 통합, + 아이콘 개선, 여백 정리 | ✅ | 탭 바 터미널 아이콘 제거(+로 통합), + 버튼을 강조 스타일(파란 테두리/배경)로 교체, 워크스페이스/그룹 상하 여백 축소·경로와 개수 한 줄로 합침 |
| 45 | 상태 | 작업중/대기 상태 감지 정확도 | ✅ | 훅(PostToolUse/PermissionRequest/Stop/SessionEnd 등 추가) + 1초 화면 검사(“esc to interrupt”, 승인 화면, 선택 질문)로 보정. Codex(node 실행)도 감지. 실제 Claude로 작업중→완료, 수동 실행 Claude도 완료 알림 확인 |
| 46 | 브랜치 | 브랜치 검색 + 페이징, 공용 모듈 | ✅ | 공용 `BranchPicker` 모듈: 검색, 로컬/원격 섹션, 스크롤 시 60개씩 페이징, 2글자 이상 검색 시 `git ls-remote`로 원격 서버 검색(미fetch 브랜치 체크아웃 지원), 새 브랜치. 분할창 브랜치 칩과 AgentGit 브랜치 메뉴가 같이 사용 |
| 47 | 창 | 여러 Agentty 창 + 창별 미니모드 | ✅ | 새 창(⇧⌘N, 터미널 메뉴): 창마다 독립 워크스페이스/레이아웃 파일, 미니모드는 창별로 옆으로 배치, 알림/신호는 해당 창으로 라우팅, 메뉴바는 모든 창 합산. 종료 시 열린 창 복원, 닫은 추가 창은 복원 안 함. 창 생성·복원 확인 |
| 48 | 사이드바 | 그룹 없는 워크스페이스가 없으면 “그룹 없음” 숨김 | ✅ | 그룹이 있어도 그룹 없는 워크스페이스가 없으면(드래그 중 제외) “그룹 없음” 섹션 숨김 |
| 49 | 분할 | 분할창마다 경로/프로젝트 표시 | ✅ | 분할창 헤더에 도구 로고 + 폴더명(넓으면 전체 경로) + 브랜치 표시 |
| 50 | 브라우저 | 인앱 브라우저를 Claude/Codex가 완전히 제어할 수 있는 API + 웹 테스트는 인앱 브라우저로 | ✅ | `agentty browser` CLI + `agentty mcp-browser` MCP 서버: open/navigate/status/wait-load/text/html/elements/click/type/press/wait/eval/console/screenshot/close. Agentty에서 실행한 Claude(--mcp-config)·Codex(mcp_servers)에 자동 연결(설정으로 끔). 로컬 서버 폼 입력·클릭·Enter 제출·콘솔·스크린샷(MCP 이미지)까지 인앱 브라우저로 검증 |

